# Configuration files

This directory contains the configuration files for each profile. 

Each profile contains the following properties:
- agent_type
- occupance_size
- nn_layers
- lr
- gamma
- batch_size
- max_memory
